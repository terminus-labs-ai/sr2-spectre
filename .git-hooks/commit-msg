#!/bin/sh
# Installed by harbinger (hrb-33). Appends the bead id to commit messages
# that omit it so the commit-evidence check passes deterministically.
BEAD="obsidian-hx00"
if ! grep -qF "$BEAD" "$1"; then
    sed -i "1s/$/ ($BEAD)/" "$1"
fi
